var PhotoInputModel = (function() {
    function PhotoInputModel(id,title,photoBase) {
        this._id = id;
        this.title = title;
        this.photoBase = photoBase;
    }

    return PhotoInputModel;
}());